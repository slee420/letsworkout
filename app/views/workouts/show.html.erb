<div>
  <div>
    <h1>
      <%= @the_workout.muscle_target %> Workout on <%= @the_workout.date.strftime("%b %d, %Y") %> Details
    </h1>

    <div>
      <div>
        <a href="/workouts">
          Go back
        </a>
      </div>

      <div>
        <a href="/delete_workout/<%= @the_workout.id %>">
          Delete workout
        </a>
      </div>
    </div>

    <dl>
      <dt>
        Date
      </dt>
      <dd>
        <%= @the_workout.date.strftime("%b %d, %Y") %>
      </dd>

      <dt>
        Muscle Target
      </dt>
      <dd>
        <%= @the_workout.muscle_target %>
      </dd>

      <dt>
        Quality
      </dt>
      <dd>
        <%= @the_workout.quality %> out of 5
      </dd>

      <dt>
        Location
      </dt>
      <dd>
        <%= @the_workout.location %>
      </dd>

      <dt>
        Created at
      </dt>
      <dd>
        <%= time_ago_in_words(@the_workout.created_at) %> ago
      </dd>

      <dt>
        Updated at
      </dt>
      <dd>
        <%= time_ago_in_words(@the_workout.updated_at) %> ago
      </dd>
    </dl>
  </div>
</div>

<hr>


<div>
  <div>
    <h2>
      Edit Workout
    </h2>

    <form action="/modify_workout/<%= @the_workout.id %>"  method="post" >
      <div>
        <label for="date_box">
          Date
        </label>

        <input type="date" id="date_box" name="query_date" value="<%= @the_workout.date %>">
      </div>

      <div>
        <label for="muscle_target_box">
          Muscle target
        </label>

        <input type="text" id="muscle_target_box" name="query_muscle_target" value="<%= @the_workout.muscle_target %>">
      </div>

      <div>
        <label for="quality_box">
          Quality
        </label>

        <input type="text" id="quality_box" name="query_quality" value="<%= @the_workout.quality %>">
      </div>

      <div>
        <label for="location_box">
          Location
        </label>

        <input type="text" id="location_box" name="query_location" value="<%= @the_workout.location %>">
      </div>

      <button>
        Update Workout
      </button>
    </form>
  </div>
</div>

<h2>Exercises</h2>
<table border="1">
  <tr>
    <th>
      ID
    </th>

    <th>
      Exercise Name
    </th>

    <th>
      Weights
    </th>

    <th>
      Reps
    </th>

    <th>
      Sets
    </th>

    <th>
      Description
    </th>
    <th>
   
    </th>
  </tr>

  <% @exercises.each do |exercise| %>
  <tr>

    <td>
      <%= exercise.id %>
    </td>

    <td>
      <%= exercise.exercise_name %>
    </td>

    <td>
      <%= exercise.weight %>
    </td>
        <td>
      <%= exercise.reps %>
    </td>

        <td>
      <%= exercise.sets %>
    </td>

        <td>
      <%= exercise.description %>
    </td>
    <td>
      <a href="/exercises/<%= exercise.id %>">
        Show Details
      </a>
    </td>
  </tr>

  <% end %>

<hr>
