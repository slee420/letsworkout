
<h2>
<%= @current_user.first_name.capitalize %>'s Summary of Workouts
</h2>

<p>
Total Number of Workouts Performed: <%= @workouts.count %>
</p>
<p>
Number of Workouts Performed Last Week: <%= @list_of_workouts.where({ :date => (1.week.ago..Date.today) }).count %>
</p>
<p>
Number of Workouts Performed Two Weeks Ago: <%= @list_of_workouts.where({ :date => (1.week.ago..Date.today) }).count %>
</p>

<p>
Total Number of Exercises Performed: <%= @exercises.count %>
</p>
<p>
Total Number of Reps Performed: <%= @current_user.exercises.sum(:reps) %>
</p>
<p>
Total Number of Sets Performed: <%= @current_user.exercises.sum(:sets) %>
</p>
<p>
Last Workout Date: <%= @list_of_workouts.at(0).date.strftime("%b %d, %Y") %>
</p>


<hr>
<div>
  <div>
    <table border="1">
    <h2> All Workouts</h2>
      <tr>
 



        <th>
          Date
        </th>

        <th>
          Muscle target
        </th>

        <th>
          Quality
        </th>

        <th>
          Location
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @list_of_workouts.each do |a_workout| %>
      <tr>

        <td>
          <%= a_workout.date.strftime("%m/%d/%Y") %>
        </td>

        <td>
          <%= a_workout.muscle_target %>
        </td>

        <td>
          <%= a_workout.quality %>
        </td>

        <td>
          <%= a_workout.location %>
        </td>

        <td>
          <%= time_ago_in_words(a_workout.created_at) %> ago
        </td>
        <td>
          <%= time_ago_in_words(a_workout.updated_at) %> ago
        </td>

        <td>
          <a href="/workouts/<%= a_workout.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>
  </div>
</div>

<hr>
